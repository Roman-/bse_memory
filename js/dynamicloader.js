// const jsVersion = 38; // scripts version (force reload)

/*
// appednds scripts names to <head> so that they're loaded dynamically
function loadJsCssDynamically() {
    let jsFiles = ['global', 'init', 'helpers'];
    jsFiles.forEach(function (scriptName) {
        $("<head>").append($('<script src="js/'+scriptName+'.js?v'+jsVersion+'"> </script>'));
    });
}
*/
